import { Body1, Card, Caption1, Title1, Title3 } from '@fluentui/react-components';
import { CheckmarkCircleRegular, CodeRegular, DataAreaRegular, LayerRegular, StarRegular } from '@fluentui/react-icons';
import { useEffect, useState } from 'react';
import { api } from '../api';
import type { StackArea } from '../data/profile';
export function StackPage() { const [stack, setStack] = useState<StackArea[] | null>(null); useEffect(() => { api.getStack().then(setStack); }, []); return <div className="page-stack"><section className="page-title"><Title1><LayerRegular /> Stack & princípios</Title1><Body1>Tecnologia com intenção, qualidade que pode ser verificada.</Body1><Caption1>Mapa técnico atual</Caption1></section><div className="project-grid">{stack?.map((item, index) => <Card key={item.area} className="project-card"><Caption1>{String(index + 1).padStart(2, '0')} · {item.area}</Caption1><Title3>{item.title}</Title3><Body1>{item.technologies}</Body1><Caption1>{item.detail}</Caption1></Card>)}</div><div className="two-column"><section><div className="section-heading"><Title3><StarRegular /> Princípios de trabalho</Title3></div>{['Clareza antes de complexidade', 'Domínio protegido', 'Automação responsável', 'Qualidade verificável', 'Evolução incremental'].map((title) => <Card key={title} className="list-card"><CheckmarkCircleRegular /><div><Title3>{title}</Title3><Body1>Tornar decisões explícitas, testáveis e conectadas a resultados reais.</Body1></div></Card>)}</section><section><div className="section-heading"><Title3><CodeRegular /> Áreas atuais</Title3></div>{['Arquitetura de software e sistemas distribuídos', 'Engenharia de plataformas, observabilidade e confiabilidade', 'Inteligência artificial aplicada a produtos e operações', 'Automação de workflows com agentes', 'Liderança técnica, qualidade e evolução organizacional'].map((area) => <Card key={area} className="list-card"><DataAreaRegular /><Body1>{area}</Body1></Card>)}<Card className="empty-state"><LayerRegular /><Title3>Atividade recente</Title3><Body1>Ainda não há atualizações públicas para exibir neste painel.</Body1></Card></section></div></div>; }